Use patient_records.csv as flat file input in the data flow task.
Use the SQL scripts to create staging table for customers of all country and to create a stored procedure which will be used later in the data flow task of SSIS package to create table based on country name dynamically.
Open the HospitalDataManagement project in SSIS and after setting all the source and target connections properly, execute the package. 
